Version 1.0.4: actual pinned Player transition/seek tests and existing queue/startup/drain/progress regressions passed; JavaScript syntax checked; JSON/YAML parsed; ContinuousSession unchanged. No live phone/hub test or Docker image build. npm test runs both included suites after dependencies are installed.
